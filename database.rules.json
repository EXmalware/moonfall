{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null",
        "players": {
          "$playerId": {
            ".validate": "newData.hasChildren(['id', 'alias', 'alive'])"
          }
        }
      }
    }
  }
}
